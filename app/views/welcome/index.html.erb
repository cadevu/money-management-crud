<h1>Monthly financial control!</h1>
<h3> Total spent : R$<%= Expense.sum(:amount)%></h3>
<h3>Largest expense : <%=Expense.where(amount: Expense.maximum(:amount)).pick(:expense)%>, amount =  R$<%=Expense.maximum(:amount) %> <h3>
<p> <p>
<%= link_to "New expense", new_expense_path %>
<p> <p>
<p> <%= link_to 'Month expenses', 'expenses#index' %> </p>

